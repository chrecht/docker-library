version: "3.8"

services:

  test-apache:
    build:
      context: .
      dockerfile: Dockerfile-test
      target: prod
    environment:
      DEBUG: dev
    ports:
      - "1180:80"
    volumes:
      - ./html:/var/www/html
